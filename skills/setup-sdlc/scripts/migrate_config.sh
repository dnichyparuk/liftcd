#!/usr/bin/env bash
# Legacy migration support has been removed.
# Config migration is no longer needed — LiftCD only supports the current schema.
echo "[liftcd] migrate-config: no-op (legacy migration support removed)" >&2
exit 0
